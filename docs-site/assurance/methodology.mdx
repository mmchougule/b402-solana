---
title: Assurance Methodology
description: "How b402-solana's separate assurance workspace verifies the production stack."
---

`b402-solana-assurance` is an isolated workspace whose sole purpose is
to verify, stress-test, and benchmark the production `b402-solana`
implementation **without modifying it**. It treats the main repo as
the immutable system under test.

Source: [`b402-solana-assurance`](https://github.com/b402-ai/b402-solana-assurance)

## Why isolated

Per the workspace README:

> - the active repo is under concurrent development
> - SBF builds are sensitive to dependency and stack changes
> - fuzz corpora, benchmark baselines, and generated reports should be
>   kept deliberate and reviewable

Practical implication: the assurance repo never edits the production
crate sources or program code. It pulls them as path dependencies,
adds parity reference implementations, runs property/fuzz/benchmark
harnesses, and emits machine-readable artifacts plus human-readable
reports.

## PRD set

Six PRDs sequenced in order:

| PRD | Topic |
|---|---|
| [`PRD-A01`](https://github.com/b402-ai/b402-solana-assurance/blob/main/prds/PRD-A01-assurance-architecture.md) | Assurance architecture & methodology |
| [`PRD-A02`](https://github.com/b402-ai/b402-solana-assurance/blob/main/prds/PRD-A02-parity-vectors.md) | Parity vectors (Rust ↔ TS ↔ Circom) |
| [`PRD-A03`](https://github.com/b402-ai/b402-solana-assurance/blob/main/prds/PRD-A03-property-invariant-testing.md) | Property + invariant testing |
| [`PRD-A04`](https://github.com/b402-ai/b402-solana-assurance/blob/main/prds/PRD-A04-fuzzing.md) | Fuzzing |
| [`PRD-A05`](https://github.com/b402-ai/b402-solana-assurance/blob/main/prds/PRD-A05-benchmarks-budgets.md) | Benchmarks + budgets |
| [`PRD-A06`](https://github.com/b402-ai/b402-solana-assurance/blob/main/prds/PRD-A06-formal-verification-readiness.md) | Formal verification readiness |

## Inputs

The assurance workspace imports `@b402ai/solana-shared` as a workspace
file dep and reads path-relative TypeScript sources directly from
`../b402-solana/`:

- `circuits/` — circuit sources + build artifacts
- `packages/crypto/` — Rust Fr / Poseidon / Merkle reference
- `packages/prover/` — TS prover wrapper (imported as
  `TransactProver`)
- `packages/sdk/` — TS SDK (imported for `commitmentHash`,
  `nullifierHash`, `merkleNodeHash`, `ClientMerkleTree`,
  `buildWallet`, `encryptNote`, `tryDecryptNote`)
- `programs/b402-verifier-transact/` — Rust verifier crate (built as
  the `b402-verify-cli` binary for parity checks)
- `programs/b402-verifier-adapt/` — Rust verifier-adapt crate (its
  own `cargo test --tests` is invoked by the harness)
- `programs/b402-pool/` — Pool program (LiteSVM probe path)
- `programs/b402-jupiter-adapter/` — Jupiter adapter

It does not modify any of these.

## Source map

| Harness file | Generates | PRD |
|---|---|---|
| [`src/index.ts`](https://github.com/b402-ai/b402-solana-assurance/blob/main/src/index.ts) | top-level orchestrator: builds Rust helpers, generates `vectors/`, dispatches sub-harnesses, writes `reports/assurance-summary.md` | A01 |
| [`src/adapt.ts`](https://github.com/b402-ai/b402-solana-assurance/blob/main/src/adapt.ts) | `artifacts/adapt-assurance.json`, runs `cargo test` on `b402-verifier-adapt` | A02 |
| [`src/invariants.ts`](https://github.com/b402-ai/b402-solana-assurance/blob/main/src/invariants.ts) | `artifacts/invariant-summary.json`: 128×4 property runs + 64+32+8 stateful sequences | A03 |
| [`src/fuzz.ts`](https://github.com/b402-ai/b402-solana-assurance/blob/main/src/fuzz.ts) | `artifacts/fuzz-summary.json`: 256 Fr / 128 proof-mut / 128 PI-mut / 8 malformed / 32 note-payload mutations | A04 |
| [`src/bench.ts`](https://github.com/b402-ai/b402-solana-assurance/blob/main/src/bench.ts) | `artifacts/bench-summary.json` + `baselines/{cubench,microbench,proofbench,scanbench}.json` | A05 |
| [`src/onchain.ts`](https://github.com/b402-ai/b402-solana-assurance/blob/main/src/onchain.ts) | `artifacts/onchain-summary.json` + `artifacts/onchain-compute-units.json` from LiteSVM probe | — |
| [`src/fv.ts`](https://github.com/b402-ai/b402-solana-assurance/blob/main/src/fv.ts) | `reports/fv/` formal-verification handoff bundle | A06 |
| [`src/adapt-litesvm-fixture.ts`](https://github.com/b402-ai/b402-solana-assurance/blob/main/src/adapt-litesvm-fixture.ts) | `artifacts/adapt-litesvm-fixture.json` for the on-chain probe | A02/A03 |

## Outputs

After a full assurance run (`pnpm assure`), the workspace emits:

```
artifacts/
  shield-witness.json
  proof-input.json
  proof-verification.json
  adapt-assurance.json
  invariant-summary.json
  fuzz-summary.json
  onchain-summary.json
  bench-summary.json

vectors/
  domain-tags.json
  fr-codec.json
  poseidon-primitives.json
  merkle.json
  note-encryption.json

reports/
  assurance-summary.md
  parity-summary.md
  invariant-summary.md
  fuzz-summary.md
  onchain-summary.md
  benchmark-summary.md
  adapt-summary.md
  fv/
```

The `vectors/` files are pinned: any change to circuit semantics that
mutates these requires a deliberate update commit. They are the
parity gold standard.

## What "assurance pass" means

Generated [`reports/assurance-summary.md`](https://github.com/b402-ai/b402-solana-assurance/blob/main/reports/assurance-summary.md)
from the 2026-04-25T23:06Z run:

```
Phase 2a status:
  Real ZK-bound adapt evidence is present through the adapt fixture and
  verifier-adapt Rust tests.
  Adapt verifier tests:                  6 passed, 0 failed
  Adapt public inputs:                   23
  Adapt ceremony:                        throwaway-devnet-only
  Adapt proof SHA-256:                   142c6007ee916a67d0c5f3cec6523907f121490b57967e3aa0d1d147fec7ae1c

Scale:
  Wallet determinism runs:               128
  Wallet separation runs:                128
  Note round-trip runs:                  128
  Merkle property sequences:              32
  Stateful sequences (10/50/150 actions):  64 / 32 / 8 = 104
  Root pressure appends:                 512
  Nullifier shard samples:               4096; max bucket 4

Fuzz and adversarial:
  Fr canonical accepts:                   43 / 256
  Fr canonical rejects:                  213 / 256
  Proof mutation rejects:                128 / 128
  Public-input mutation rejects:         128 / 128
  Malformed verifier-input rejects:        8 / 8
  Note-store malformed rejects:           32 / 32
  Adapt binding mutation cases tracked:    6
  Non-rejecting proof mutations:           0

On-chain LiteSVM (assurance-side probe):
  shield_success CU:                     233,495
  unshield_success CU:                   223,413
  adapt_execute_full_path CU:            313,725
  Main-repo on-chain tests:              12 passed, 0 failed, 3 deferred
  Full adapt_execute LiteSVM coverage:   present
  Adapt verifier configured:             yes

Benchmarks (Apple M4 Pro, Node v24.4.0):
  Transact proof generation avg ms:      733.357
  Verifier CLI avg ms:                   116.791
  Wallet build avg ms:                   0.740
  Merkle append avg ms:                  3.797
  Note encrypt + decrypt avg ms:         3.064
  Note scan + decrypt avg ms:            3.086
  Adapt fixture load avg ms:             0.093
```

Numbers come from a full machine-emitted run; sources cited in
`artifacts/*.json`.

## Mainnet readiness gate

Per the assurance summary's "Current audit-readiness gaps" section:

```
ONCHAIN-ADAPT-FULLPATH         [covered]
ONCHAIN-ADAPT-VERIFIER-ID      [covered]
ONCHAIN-ADAPT-DELTA-DEFERRED   [stale-doc]
ONCHAIN-PROBE-STATUS           [covered]
```

Mainnet remains blocked on:

- Multi-party trusted-setup ceremony (not started — calendar item).
- External audits (Veridise, Accretion, Zellic — not scheduled).
- Solana-native formal verification (PRD-A06 specs the readiness
  artifacts; FV engagement not started).

These gates are not assurance-side; they are external. Assurance
documents that the *engineering surface* is ready for them.

## How "external findings" work

The assurance workspace explicitly does not modify the production
code, so when it identifies a gap it tags the finding rather than
fixing it directly. The format from
[`reports/onchain-summary.md`](https://github.com/b402-ai/b402-solana-assurance/blob/main/reports/onchain-summary.md):

```
ONCHAIN-ADAPT-FULLPATH         [covered]
ONCHAIN-ADAPT-VERIFIER-ID      [covered]
ONCHAIN-ADAPT-DELTA-DEFERRED   [stale-doc]
```

Tags:

- `[covered]` — assurance verified the property; no main-repo change
  needed.
- `[gap/<severity>]` — main repo has a real gap; flagged for the team.
- `[stale-doc/<severity>]` — main repo's docs/comments lag behind
  current code state.

Example: in the current run, the main repo's on-chain harness
initializes `verifier_adapt` with the transact verifier ID — a
LiteSVM test setup mistake, not an on-chain bug. Assurance flagged
it as `[covered]` because the assurance probe runs the *correct*
configuration with full coverage. The main-repo fix is editorial.

## Workflow

For the system-under-test team:

1. Land a change in `b402-solana` (circuit, program, SDK, etc.).
2. The assurance repo re-runs `pnpm assure` against the new
   path-dependency. The single entrypoint
   ([`src/index.ts`](https://github.com/b402-ai/b402-solana-assurance/blob/main/src/index.ts))
   builds Rust verifier helpers, regenerates `vectors/`, exercises the
   property/invariant/fuzz/bench/onchain harnesses, and writes
   `artifacts/` + `reports/`.
3. New findings get filed; existing findings get re-categorized
   (`[gap]` → `[covered]` once fixed; `[covered]` → `[gap]` if a
   regression).
4. Reports are reviewed at PR time; ad-hoc snapshots are taken before
   each release.

For audit-grade fuzz depth, `pnpm assure:audit-fuzz` runs the same
entrypoint with `B402_AUDIT_FUZZ=1` set, raising fuzz iteration counts
beyond the gate-profile defaults.

For external auditors:

1. The assurance repo's `vectors/` + `reports/` + `artifacts/` are
   the canonical handoff package.
2. Reproducible by `pnpm assure` against a specific
   `b402-solana` commit hash.
3. PRD-A06 specs the additional FV-readiness artifacts (canonical
   semantics, reduction proofs, adversary models) the audit team
   will ingest. Generated bundle lands under `reports/fv/`.

## Continue reading

<CardGroup cols={2}>
  <Card title="Parity Vectors" icon="equals" href="/assurance/parity-vectors">
    Rust ↔ TypeScript ↔ Circom byte-equality.
  </Card>
  <Card title="Property Invariants" icon="vial" href="/assurance/property-invariants">
    Stateful sequence checks + replay rejection.
  </Card>
  <Card title="Fuzzing" icon="bug" href="/assurance/fuzzing">
    Adversarial proof and public-input mutation.
  </Card>
  <Card title="Benchmarks" icon="gauge" href="/assurance/benchmarks">
    CU and tx-size budgets.
  </Card>
</CardGroup>
