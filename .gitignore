node_modules/
dist/
target/
.anchor/
.DS_Store
*.log
logs/

# Next.js build artifacts (apps/playground)
.next/
next-env.d.ts
*.tsbuildinfo

# Circuits copied into mcp-server at pack time. Source-of-truth lives at
# circuits/build/ — packages/mcp-server/circuits/ is reproduced by
# prepublishOnly. Never commit duplicates.
packages/mcp-server/circuits/

# Local skill installs / parallel agent staging — user-machine-specific.
.agents/
.codex-staging/
skills-lock.json

# Anchor / Solana
test-ledger/
*.so
**/target/

# Circom intermediates — only the pinned prover artifacts below are tracked.
circuits/**/*.r1cs
circuits/**/*.r1cs.json
circuits/**/*.sym
circuits/**/witness.wtns
circuits/build/**/*.cpp
circuits/build/**/*.hpp
circuits/build/**/*.c
circuits/build/**/*.dat
circuits/build/**/generate_witness.js
circuits/build/**/witness_calculator.js
circuits/build/test_artifacts/
circuits/build/ceremony/*_0000.zkey
circuits/build/ceremony/*_0001.zkey
circuits/build/ceremony/*.zkey.tmp
circuits/build/ceremony/*.ptau
# adapt_v2 is on a feature branch; keep its build artifacts local-only.
circuits/build/adapt_v2_js/
circuits/build/ceremony/adapt_v2_*
circuits/build/adapt_v2.r1cs.json
circuits/build/adapt_v2.r1cs.info.txt
# Tracked: circuits/build/{transact,adapt}_js/*.wasm
#          circuits/build/ceremony/{transact,adapt}_final.zkey
#          circuits/build/ceremony/*_verification_key.json
# These are throwaway-devnet-only ceremony outputs (single contributor);
# mainnet re-runs the ceremony per PRD-08, see docs/protocol/verifier.mdx.

# snarkjs / ptau intermediate
ops/ceremony/*.ptau
ops/ceremony/*.zkey.tmp

# env
.env
.env.*
!.env.example

# internal docs — never commit
DEMO.md
OPS.md
NOTES.md
SCRATCH.md
docs/SUBMISSION.md

# Deploy authority keypairs — never commit, back up locally.
ops/keypairs/
BUILD-STATE.md
ENGINEER-REVIEW.md

# Sub-agent scratch
.copilot-out/
docs/LAUNCH-DRAFTS.md

# Engineering spikes — throwaway design docs, runbooks, exploratory PRDs.
# Spikes that mature into committed work get promoted to docs/prds/PRD-NN-...md.
docs/spikes/

# Internal ops state docs — never push to public repo. Use locally for handoff.
ops/MAINNET-DEPLOY-RUNBOOK.md
ops/MAINNET-STATE.md
ops/PHASE-*.md

# Local demo helper — hardcoded technical commentary not for public ridicule.
# Source-of-truth for protocol facts is docs/ + circuits/ + the assurance repo.
scripts/watch-logs.mjs
